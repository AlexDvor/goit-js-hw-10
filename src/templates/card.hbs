{{!-- {{#each this}}
<li class="menu__item">
    <img src="{{flag}}" alt="{{name}}" width="120px" >
    <h1>{{name}}</h1>
    <h2>Capital: {{capital}}</h2>
    <p>Population: {{population}}</p>
    {{#each this.languages}}
    <p>Languages: {{name}}</p>
    {{/each}}
</li>
{{/each}} --}}


{{#each this}}
<li class="menu__item">
    <img src="{{flag}}" alt="{{name}}" width="120px">
    <h1>{{name}}</h1>
    <h2>Capital: {{capital}}</h2>
    <p>Population: {{population}}</p>
    <p>Languages:{{#each this.languages}} {{name}}, {{/each}}</p>
   
</li>
{{/each}}

